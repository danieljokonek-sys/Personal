# Email Cleanup Bot — Configuration

owner:
  name: "Daniel Jokonek"

account:
  email: "danieljokonek@gmail.com"
  token_file: "credentials/token.json"

google:
  credentials_file: "credentials/credentials.json"

organizer:
  model: "claude-sonnet-4-6"
  batch_size: 25
